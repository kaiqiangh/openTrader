# P4-007 P4-008 P5-001 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Add local setup troubleshooting documentation and implement execution metrics/tracing (`P4-007`), mode integration tests (`P4-008`), and OMS lifecycle state machine (`P5-001`).

**Architecture:** Extend execution runtime with explicit metrics/tracing collector interfaces and integrate them into simulation and real execution worker paths. Add integration tests that validate strict mode separation and lifecycle event expectations. Implement a standalone OMS state machine module with deterministic transition validation and idempotent no-op handling for repeated terminal updates.

**Tech Stack:** Python 3.13, Go 1.21+, pytest, go test, Markdown docs.

---

### Task 1: Root README local setup and troubleshooting

**Files:**
- Modify: `README.md`

**Step 1: Write the failing test**

```python
def test_readme_includes_local_setup_and_troubleshooting():
    ...
```

**Step 2: Run test to verify it fails**

Run: `rg -n "Local Setup|Troubleshooting|ruff|pytest|uv" README.md`
Expected: missing detailed local troubleshooting section.

**Step 3: Write minimal implementation**

- Add clear local setup section for macOS/Linux terminal.
- Add explicit explanation of why commands can pass in Codex environment while failing locally.
- Add exact command troubleshooting checklist for `uv`, `ruff`, `pytest`, and Go cache permissions.

**Step 4: Run test to verify it passes**

Run: `rg -n "Local Setup|Troubleshooting|Why it works in Codex|ruff|pytest|GOCACHE" README.md`
Expected: all required guidance present.

**Step 5: Commit**

```bash
git add README.md
git commit -m "docs: add local setup and terminal troubleshooting guide"
```

### Task 2: Execution metrics and tracing (`P4-007`)

**Files:**
- Create: `services/simulation_execution/metrics_tracing.py`
- Modify: `services/simulation_execution/worker.py`
- Create: `services/real_execution_go/internal/metrics/collector.go`
- Modify: `services/real_execution_go/internal/service/runner.go`
- Test: `tests/test_p4_execution_metrics.py`
- Test: `services/real_execution_go/internal/metrics/collector_test.go`

**Step 1: Write the failing test**

```python
def test_simulation_worker_records_latency_and_failures():
    ...
```

**Step 2: Run test to verify it fails**

Run: `uv run pytest tests/test_p4_execution_metrics.py -q`
Expected: FAIL because metrics module/hooks do not exist.

Run: `cd services/real_execution_go && GOCACHE=/tmp/go-build go test ./...`
Expected: FAIL for missing metrics collector package.

**Step 3: Write minimal implementation**

- Add Python execution metrics collector for simulation worker stage runs/failures/latency.
- Integrate simulation worker to record success/failure spans.
- Add Go metrics collector for real execution runner loop counters/latency snapshots.

**Step 4: Run test to verify it passes**

Run: `uv run pytest tests/test_p4_execution_metrics.py -q`
Run: `cd services/real_execution_go && GOCACHE=/tmp/go-build go test ./...`
Expected: PASS.

**Step 5: Commit**

```bash
git add services/simulation_execution/metrics_tracing.py services/simulation_execution/worker.py services/real_execution_go/internal/metrics/collector.go services/real_execution_go/internal/service/runner.go tests/test_p4_execution_metrics.py services/real_execution_go/internal/metrics/collector_test.go
git commit -m "feat(p4-007): add execution metrics and tracing hooks for mock/real engines"
```

### Task 3: Mode integration tests (`P4-008`)

**Files:**
- Create: `tests/test_p4_mode_integration.py`

**Step 1: Write the failing test**

```python
async def test_mock_worker_processes_mock_queue_and_never_real_queue():
    ...
```

**Step 2: Run test to verify it fails**

Run: `uv run pytest tests/test_p4_mode_integration.py -q`
Expected: FAIL because integration tests do not exist.

**Step 3: Write minimal implementation**

- Add integration tests validating:
  - strict mock/real queue separation
  - mock lifecycle event emission (`created`, `filled`, `ignored` paths)
  - mode leakage rejection behavior.

**Step 4: Run test to verify it passes**

Run: `uv run pytest tests/test_p4_mode_integration.py -q`
Expected: PASS.

**Step 5: Commit**

```bash
git add tests/test_p4_mode_integration.py
git commit -m "test(p4-008): add mode integration tests for mock/real separation"
```

### Task 4: OMS lifecycle state machine (`P5-001`)

**Files:**
- Create: `services/oms/state_machine.py`
- Create: `tests/test_p5_oms_state_machine.py`
- Modify: `services/oms/AGENT.md` (if needed for state-machine references)

**Step 1: Write the failing test**

```python
def test_oms_state_machine_valid_transition_sequence():
    ...
```

**Step 2: Run test to verify it fails**

Run: `uv run pytest tests/test_p5_oms_state_machine.py -q`
Expected: FAIL because OMS state machine module does not exist.

**Step 3: Write minimal implementation**

- Implement deterministic transition map for required OMS states.
- Add transition validator and apply method.
- Handle invalid transitions with explicit error and allow idempotent no-op replays for same state.

**Step 4: Run test to verify it passes**

Run: `uv run pytest tests/test_p5_oms_state_machine.py -q`
Expected: PASS.

**Step 5: Commit**

```bash
git add services/oms/state_machine.py tests/test_p5_oms_state_machine.py services/oms/AGENT.md
git commit -m "feat(p5-001): add OMS lifecycle state machine with transition guards"
```

### Task 5: Plan/doc updates and validation summary

**Files:**
- Modify: `docs/IMPLEMENTATION_PLAN.md`
- Modify: `docs/real_execution_go_baseline.md`
- Modify: `docs/agent_runtime_baseline.md`

**Step 1: Write the failing test**

```python
def test_implementation_plan_updates_p4_007_p4_008_p5_001_status():
    ...
```

**Step 2: Run test to verify it fails**

Run: `rg -n "P4-007|P4-008|P5-001|Turn Update" docs/IMPLEMENTATION_PLAN.md`
Expected: statuses not updated for current turn.

**Step 3: Write minimal implementation**

- Update progress ledger and turn update block.
- Move task statuses for completed items.
- Update immediate next actions accordingly.
- Add short notes in runtime baseline docs for new metrics and OMS lifecycle module.

**Step 4: Run test to verify it passes**

Run: `rg -n "P4-007|P4-008|P5-001|Immediate Next Actions" docs/IMPLEMENTATION_PLAN.md docs/real_execution_go_baseline.md docs/agent_runtime_baseline.md`
Expected: updated references present.

**Step 5: Commit**

```bash
git add docs/IMPLEMENTATION_PLAN.md docs/real_execution_go_baseline.md docs/agent_runtime_baseline.md
git commit -m "docs: update plan and runtime baselines for p4-007 p4-008 p5-001"
```

## Execution Log (2026-02-14)

- Task 1 status: Completed.
- Task 2 status: Completed.
- Task 3 status: Completed.
- Task 4 status: Completed.
- Task 5 status: Completed.
- Notes:
  - User requested local setup documentation and immediate progression on `P4-007`, `P4-008`, `P5-001`.
  - Work completed in strict order: docs unblock, execution metrics/tracing, mode integration tests, OMS state machine, then plan/docs updates.
  - Validation passed:
    - `uv run ruff check .`
    - `pytest -q`
    - `cd services/real_execution_go && GOCACHE=/tmp/go-build go test ./...`
  - Additional environment note:
    - `uv run pytest -q` can fail in this local setup when `pytest-asyncio` is not installed in `.venv`; workaround documented in `README.md`.

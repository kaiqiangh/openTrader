route:
  receiver: default
  group_by: ["alertname", "service"]
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 2h

receivers:
  - name: default
    webhook_configs:
      - url: "http://127.0.0.1:65535/"
        send_resolved: true
